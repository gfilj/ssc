package com.project.schedule.ssc.zhongsan;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
public class zhixuan{
 private Logger logger = LoggerFactory.getLogger(getClass());
	public boolean fushi(String betStr, String winStr) {
logger.info("fushi betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean danshi(String betStr, String winStr) {
logger.info("danshi betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean hezhi(String betStr, String winStr) {
logger.info("hezhi betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
}