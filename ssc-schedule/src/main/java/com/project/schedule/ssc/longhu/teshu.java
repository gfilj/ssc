package com.project.schedule.ssc.longhu;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
public class teshu{
 private Logger logger = LoggerFactory.getLogger(getClass());
	public boolean wq(String betStr, String winStr) {
logger.info("wq betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean wb(String betStr, String winStr) {
logger.info("wb betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean ws(String betStr, String winStr) {
logger.info("ws betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean wg(String betStr, String winStr) {
logger.info("wg betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean qb(String betStr, String winStr) {
logger.info("qb betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean qs(String betStr, String winStr) {
logger.info("qs betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean qg(String betStr, String winStr) {
logger.info("qg betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean bs(String betStr, String winStr) {
logger.info("bs betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean bg(String betStr, String winStr) {
logger.info("bg betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean sg(String betStr, String winStr) {
logger.info("sg betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
}