package com.project.schedule.ssc.budingwei;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
public class wuxingbudingwei{
 private Logger logger = LoggerFactory.getLogger(getClass());
	public boolean wuxingyimabudingwei(String betStr, String winStr) {
logger.info("wuxingyimabudingwei betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean wuxingermabudingwei(String betStr, String winStr) {
logger.info("wuxingermabudingwei betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean wuxingsanmabudingwei(String betStr, String winStr) {
logger.info("wuxingsanmabudingwei betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
}