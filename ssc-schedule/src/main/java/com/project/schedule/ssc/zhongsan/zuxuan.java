package com.project.schedule.ssc.zhongsan;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
public class zuxuan{
 private Logger logger = LoggerFactory.getLogger(getClass());
	public boolean zusan(String betStr, String winStr) {
logger.info("zusan betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean zuliu(String betStr, String winStr) {
logger.info("zuliu betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean zusandt(String betStr, String winStr) {
logger.info("zusandt betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean zuliudt(String betStr, String winStr) {
logger.info("zuliudt betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean hunhezuxuan(String betStr, String winStr) {
logger.info("hunhezuxuan betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
	public boolean hezhi(String betStr, String winStr) {
logger.info("hezhi betStr：" + betStr + ",winStr:" + winStr);
		return false;
	}
}